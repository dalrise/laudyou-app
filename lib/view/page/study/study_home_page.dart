import 'package:flutter/cupertino.dart';

class StudyHomePage extends StatelessWidget {
  static String routeName = "study-home";

  const StudyHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
